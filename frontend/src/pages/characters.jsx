import { useEffect, useState } from "react";

function characters() {
    const [character, setCharacters] = useState([]);

    return(
        <div>

        </div>
    );
}

export default characters;